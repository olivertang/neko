import type { EntryPoint } from "permissionless/types";
import { type Address, type Chain, type Client, type PublicActions, type PublicRpcSchema, type Transport } from "viem";
import type { KernelPluginManager, KernelPluginManagerParams } from "../../../types/kernel.js";
import type { KernelSmartAccount } from "../createKernelAccount.js";
/**
 * Default addresses for kernel smart account
 */
export declare const KERNEL_ADDRESSES: {
    FACTORY_ADDRESS: Address;
    ENTRYPOINT_V0_6: Address;
};
/**
 * Build a kernel smart account from a private key, that use the ECDSA signer behind the scene
 * @param client
 * @param privateKey
 * @param entryPoint
 * @param index
 * @param factoryAddress
 * @param ecdsaValidatorAddress
 * @param deployedAccountAddress
 */
export declare function createKernelAccountV0_2<entryPoint extends EntryPoint, TTransport extends Transport = Transport, TChain extends Chain | undefined = Chain | undefined>(client: Client<TTransport, TChain, undefined, PublicRpcSchema, PublicActions<TTransport, TChain>>, { plugins, entryPoint: entryPointAddress, index, factoryAddress, deployedAccountAddress }: {
    plugins: Omit<KernelPluginManagerParams<entryPoint>, "entryPoint" | "kernelVersion"> | KernelPluginManager<entryPoint>;
    entryPoint: entryPoint;
    index?: bigint;
    factoryAddress?: Address;
    deployedAccountAddress?: Address;
}): Promise<KernelSmartAccount<entryPoint, TTransport, TChain>>;
//# sourceMappingURL=createKernelAccountV0_2.d.ts.map