import { FC } from 'react';
import { Core } from '@dynamic-labs/client';
type WebViewProps = {
    webviewUrl: string;
    core: Core;
    webviewDebuggingEnabled?: boolean;
};
export declare const WebView: FC<WebViewProps>;
export declare const createWebView: (props: WebViewProps) => () => JSX.Element;
export {};
